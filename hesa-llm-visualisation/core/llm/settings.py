# from .config import ModelConfig

# # Development configuration (faster, less memory)
# CURRENT_CONFIG = ModelConfig.get_development_config()

# # Production mode (full model, GPU optimized)
# # CURRENT_CONFIG = ModelConfig.get_production_config() 